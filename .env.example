# Web server port (default: 5173)
PORT=5173

# Enable filesystem watcher for live SSE updates (1=on, 0=off)
REPOGRAPH_WATCH=1

# Comma-separated list of allowed root paths for analysis (empty = current dir only)
REPOGRAPH_ALLOWED_ROOTS=
